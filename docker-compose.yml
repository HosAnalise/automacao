services:
  ? ''
  : build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        PYTESTMARK: ''
    image: hosanalise/:latest
    command:
    - python
    - scheduler.py
    - --tarefa
    - ''
  dockercontaspagar:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        PYTESTMARK: dockerContasPagar
    image: hosanalise/dockercontaspagar:latest
    command:
    - python
    - scheduler.py
    - --tarefa
    - dockerContasPagar
